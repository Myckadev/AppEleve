<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Application Activité |{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_front') }}">Activité</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_front') }}">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=""></a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Compétence</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('competence_new') }}">Ajouter</a>
                        <a class="dropdown-item" href="{{ path('competence_index') }}">Liste</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Contenu</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('contenu_new') }}">Ajouter</a>
                        <a class="dropdown-item" href="{{ path('contenu_index') }}">Liste</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Activité</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('activite_new') }}">Ajouter</a>
                        <a class="dropdown-item" href="{{ path('activite_index') }}">Liste</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Élève</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('eleve_new') }}">Ajouter</a>
                        <a class="dropdown-item" href="{{ path('eleve_index') }}">Liste</a>
                    </div>
                </li>

            </ul>
        </div>
    </div>
</nav>
{% block body %}
{% endblock %}

<script src="{{ asset('assets/js/bootstrap.js') }}"></script>
</body>
</html>
